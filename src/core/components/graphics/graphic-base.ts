import {Vec2d} from '../../../utils/vec2d';

export type Color = number;

export type GraphicBase = {key: string; pos: Vec2d; zIndex: number};

export type CanvasGraphicBase = {key: string; zIndex: number};
